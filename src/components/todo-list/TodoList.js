import React from 'react'
import { withRouter } from 'react-router-dom'


const TodoList = () => {
    return (
        <div>This page will host the to do list app...</div>
    );
}

export default withRouter(TodoList);