import React from 'react';
import { withRouter } from 'react-router-dom'



const ShoppingList = () => {
    return (
        <div>This page will host the shopping list app...</div>
    );
}

export default withRouter(ShoppingList);